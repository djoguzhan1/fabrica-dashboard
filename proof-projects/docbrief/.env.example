# Copy to .env and fill in ONE of these. Load it with `set -a; . ./.env; set +a`
# or your shell's dotenv tool. Never commit .env (it is in .gitignore).
OPENAI_API_KEY=
ANTHROPIC_API_KEY=

# Optional defaults
DOCBRIEF_PROVIDER=openai
DOCBRIEF_MODEL=
